### Keyring
A keyring in Linux is a secure system that stores sensitive data, like passwords, Wi-Fi keys, and digital certificates.

### Tee 
The Linux tee command reads standard input and writes it to both standard putput and one or more files simultaneously