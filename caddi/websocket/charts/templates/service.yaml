apiVersion: v1
kind: Service
metadata:
  name: {{ include "caddiwebsocket.fullname" . }}
  labels:
    {{- include "caddiwebsocket.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caddiwebsocket.selectorLabels" . | nindent 4 }}
