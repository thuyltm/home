1. Download **btcd-linux-amd64-v0.25.0.tar.gz** from the github https://github.com/btcsuite/btcd/releases
2. Extract the archieve and move the executable files to the __/usr/local/bin/__ directoy
```sh
sudo mv btcctl /usr/local/bin
sudo mv btcl /usr/local/bin
```